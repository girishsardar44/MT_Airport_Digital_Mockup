from json import JSONEncoder
 
class gjc:

    def geometry(type, coordinates):
        type = str(type)
        coordinates = [coordinates]

    def feature(geometry, properties={}):
        type = "Feature",
        properties = properties,
        geometry = {
            geometry
        }

